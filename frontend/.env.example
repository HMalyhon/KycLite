# Optional frontend configuration. Copy to ".env" to override.

# Backend the Vite dev proxy forwards /api to.
VITE_API_TARGET=http://localhost:5000

# Optional absolute API base for the client (leave empty to use the dev proxy).
VITE_API_BASE=
